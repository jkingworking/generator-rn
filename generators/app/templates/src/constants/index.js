// These actions can be dispatched from this component but will be handled in the parent application.
export const DEFAULT_ERROR = 'DEFAULT_ERROR';
export const ASYNC_STATUSES = { DEFAULT: 'DEFAULT', ERROR: 'ERROR', SUCCESS: 'SUCCESS', IN_PROGRESS: 'IN_PROGRESS'};

// These actions are handled by this component
export const <%= componentName.constCase %> = {
	/* =- yoemanHook:constantList -= */
};
