/* eslint-env mocha */
/* eslint-disable padded-blocks, no-unused-expressions

import {expect} from 'chai';
import <%= componentName.camelCase %> from './<%= componentName.kebabCase %>.js';

describe('<%= componentName.camelCase %>', () => {
	let testComponent = <%= componentName.camelCase %>(true);

	it('should return the value passed to it', () => {
		expect(testComponent === true).to.be.ok;
	});
});
 */
